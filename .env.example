# Copy to .env and fill in.
# .env is gitignored. Do NOT commit your real token.
#
# Usage in a new shell:
#   set -a; source .env; set +a
#
# The token must belong to the jordypg account, with `repo` scope
# (and `pages` if you ever script Pages config).

GH_TOKEN=ghp_your_token_here
